<h1>Listing likes</h1>

<table>
  <thead>
    <tr>
      <th>Ll</th>
      <th>User</th>
      <th>Nick</th>
      <th>Collect_type</th>
      <th>Status</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @likes.each do |like| %>
      <tr>
        <td><%= like.ll_id %></td>
        <td><%= like.user_id %></td>
        <td><%= like.nick %></td>
        <td><%= like.collect_type %></td>
        <td><%= like.status %></td>
        <td><%= link_to 'Show', like %></td>
        <td><%= link_to 'Edit', edit_like_path(like) %></td>
        <td><%= link_to 'Destroy', like, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Like', new_like_path %>
